# Leave empty for local dev — Vite proxies /api → http://localhost:5000
# For production, point at your deployed backend origin (no trailing slash):
# VITE_API_BASE_URL=https://your-api.example.com
VITE_API_BASE_URL=
